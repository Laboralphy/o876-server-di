export enum ROLES {
    MODERATOR = 'MODERATOR', // This role allows users to moderate other users' content and messages
    GAME_MASTER = 'GAME_MASTER', // this role gives users some special power over the game
    ADMIN = 'ADMIN', // This role gives users access to administration commands for server maintenance
    SERVICE = 'SERVICE', // This role is used to automatize tasks, sending public email or announcement on chat channels
}
