# Questions

### I'd like t add a single new function in the client context

1. Go to `src/application/ports/classes/IClientContext.ts`, and add the function definition.
2. Go to `src/infrastructure/services/ApiContextBuilder.ts` and implement this new function.

