# o876-server-di




## Folders

### `./src/libs`

Contains all libraries. These folders contain the most versatile and outsider pieces of code.


## Services

Services are available to adventure scripts throught the `getService()` method.
They are written in typescript and must implement `IContextService`.
They are statically installed in-core via the `installService()` method.
